source 'https://rubygems.org'

# Required for Ruby 4.0+ (removed from standard library)
gem 'base64'
gem 'bigdecimal'
gem 'csv'
gem 'logger'

# Jekyll 4.x for Ruby 4.0 compatibility
gem 'jekyll', '~> 4.3'
gem 'jekyll-theme-minimal'
gem 'webrick'

# GitHub Pages compatible plugins
group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-seo-tag'
  gem 'jekyll-sitemap'
end
